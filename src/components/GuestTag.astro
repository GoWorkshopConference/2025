---
type Props = {
  className?: string;
};

const { className } = Astro.props;
---
<span class={`bg-blue-100 text-blue-800 rounded-full dark:bg-blue-900 dark:text-blue-300 ${className}`}>
  ゲスト企画
</span>
