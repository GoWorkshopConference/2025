---
import { type CollectionEntry } from "astro:content";
import { withBase } from "../utils/withBase";
import OrganizerCard from "./OrganizerCard.astro";

type Props = CollectionEntry<"booths">["data"] & {
  slug: string;
};

const { title, organizer, slug } = Astro.props;
---

<a href={withBase(`booths/${slug}`)} class="block h-full">
  <div class="flex flex-col gap-4 rounded shadow-lg p-8 h-full">
    <h2 class="font-bold text-2xl">
      {title}
    </h2>
    <OrganizerCard
      name={organizer.name}
      avatar={organizer.avatar}
      organization={organizer.organization}
      profile={organizer.profile}
    />
  </div>
</a>
